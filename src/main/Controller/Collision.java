package main.Controller;

public class Collision {

    boolean isCollision;

    static void detectCollision(int x1, int y1, int x2, int y2) {
        //The line on the canvas is goes from x:0 to x:1000 (max width of canvas) and is at the height of y:1075 (max height of canvas is 1200). The upper left most point on our canvas is (0|0).
    }

    void evaluate() {

    }

    public boolean isCollision() {
        return isCollision;
    }

    public void setCollision(boolean collision) {
        isCollision = collision;
    }
}
